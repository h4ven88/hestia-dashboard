# Cloudflare Pages redirects
# /app rewrites to the dashboard; root serves the landing page
# /index.html still works directly (Wall Panels, Groovy app)

/app /index.html 200
/ /hestari-landing.html 200
