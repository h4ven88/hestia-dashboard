# Cloudflare Pages redirects
# Serve landing page at root for visitors without query params
# Dashboard remains at /index.html (direct access, Wall Panels, Groovy app)

/ /hestari-landing.html 200
/app /index.html 200
